# Test Canto Mechanic

# Setup: Leif (Cavalry, Mov 7) at (1,4). Bandit at (5,4). Nanna at (1,3).

# --- TURN 1 ---

# == Test Canto after Item Use ==

# 1. Select Leif
select 1 4

# 2. Move Leif partially (e.g., 2 tiles to (3,4), Cost 2)
move 3 4
# Expect: Canto message, remaining move = 7 - 2 = 5. Leif still selected.

# 3. Use Vulnerary (Action allows Canto)
# Leif HP is likely full, but use should still work and allow Canto.
use 2 # Assuming Vulnerary is at index 2+
# Expect: Item used message. Canto message again, remaining move = 5. Leif still selected.

# 4. Move again using Canto (e.g., 3 tiles to (6,4), Cost 3. Total cost 2+3=5 <= 7)
move 6 4
# Expect: Move successful. Turn should end (no Canto after Canto move). Movement Star might activate.

# 5. Select Nanna and wait
select 1 3
wait

# 6. End Turn
endturn

# --- TURN 2 ---
# Bandit likely moved. Leif likely took poison/combat damage.

# == Test No Canto after Attack ==

# 7. Select Leif (Now likely at (6,4) or wherever Movement Star left him)
# Find Leif's current position from map render if needed. Assume (6,4) for now.
select 6 4

# 8. Move Leif partially (e.g., 2 tiles towards Bandit, assume Bandit at (4,4), move Leif to (5,4), Cost 1)
move 5 4
# Expect: Canto message, remaining move = 7 - 1 = 6. Leif still selected.

# 9. Attack Bandit (Action prevents Canto)
attack 4 4
# Expect: Combat simulation. Turn should end (no Canto after attack). Movement Star might activate.

# 10. Select Nanna and wait
# Find Nanna's position if needed. Assume (1,3).
select 1 3
wait

# 11. End Turn
endturn

quit